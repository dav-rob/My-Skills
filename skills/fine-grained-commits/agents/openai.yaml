interface:
  display_name: "Fine-grained Commits"
  short_description: "Create and push coherent Git history"
  default_prompt: "Use $fine-grained-commits to organize, commit, and regularly push the current work as coherent, reviewable units."
policy:
  allow_implicit_invocation: true
